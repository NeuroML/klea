# Session: 2026-09-08-1158

**Authoring agent:** opencode
**Model:** deepseek-v4-flash

## Objective

Land the mode plumb (ADR-0030) end-to-end and the utils/apps split
(ADR-0031) as walkthrough-reviewed PRs, backed by a new graph-level
`context` event design (ADR-0032).

## Completed

- PR 4 (committed): agent operating-mode plumbing, mode-only scope with a
  nested `Mode` model (`requested` / `resolved` / `note`). `extra_state`
  on graph runners carries the per-call request; `ModeDecision` resolves
  it at task entry; `KleaAgent.context_snapshot` projects `mode`/`note`
  into graph-level `context` stream events derived from checkpointed
  state (never node-authored -- ADR-0032 design, alternatives discarded:
  node-authored events, ownership registry, config-based mode).
  `get_allowed_msgpack_modules` registers `Mode`. Scripts/tests updated.
- `context_snapshot` kept a public hook (not `_context_snapshot`): it is a
  projection function over state that hydration code will call directly,
  and precedent `get_allowed_msgpack_modules` is the same public
  app-override pattern. Private hooks are lifecycle seams only.
- Added keyword-only `extra_state` to `run_graph_stream` and `graph_stream`
  for a uniform query-runner contract (invoke + astream_events already had
  it from PR 4).
- ADRs: wrote 0032 (graph context events + state ownership), amended
  0030 confirmation to mode-only wording, assurance/verification deferred
  to the ADR-0029 phase (per-artifact, rides node/artifact state + ADR-0013
  node events rather than the graph-level projection).
- PR 5 (committed): mode selector + resolved-mode badge in the agent web
  UI. Utils gained generic `extra` on `stream_events`/`stream_events_sync`,
  `PageContext.query_extra` + `status_extra` content slot (rendered in the
  refreshable status pane), `run_stream` forwards the extras. Agent gained
  `ui/web/mode_ui.py` (selector writes per-chat `mode_pref` + query request;
  badge mirrors resolved mode + note). SSR httpx-mocked tests for the
  extra merge.
- PR 6 (this session): refreshed `utils_pkg`/`agent_pkg`/`rag_pkg`
  AGENTS.md architecture sections (chat_core, components/, mode_router,
  per-app api + web pages); docs/api/*.rst needed no edits (automodule).
- PR 7 (committed in 4 gated groups): mode context hydration, graph-only.
  Backend: `_normalise_state_snapshot` helper shared by the stream loop,
  new generic `GET /chat/{user_id}/{chat_id}/context` (mounted in both
  apps; readiness 503, `aget_tuple` None / no checkpointer -> `{"context":
  null}` 200-not-404), agent `context_snapshot` now also projects
  `requested`. Frontend: `hydrate_chats` restores `chat["context"]`
  (setdefault merge), `mode_ui` restores the request (`mode_pref` wins >
  hydrated `requested` > general). ADR-0032 updated (hydration implemented,
  `requested` projection, stream-only gap partially addressed); why-notes
  added to `run_graph_invoke`/`run_query` docstrings. Tests: context router
  via ASGITransport + fake graph (5), agent projection unit test (2 paths).

## Errors and Lessons

- LSP diagnostics in this repo are stale/noisy (e.g. `base.py:254/369`,
  `inspector.py:94`, `test_mode_router.py:51` "after decorator"). Trust
  `ty check` (36 pre-existing baseline) + pytest instead. Verify with the
  CLI, not the LSP.
- `edit` tool matched an indented `ui.tooltip` line without error during a
  status_pane slot insertion -- always re-read the section after editing.
- Full-suite pytest runs dirty `vector_stores/*/chroma.sqlite3` binaries;
  `git restore` them after tests.
- `aget_tuple(config) -> CheckpointTuple | None` (langgraph 1.2.11) is the
  way to detect "thread never ran" vs projecting state-schema defaults.

## Open Questions

- CLI `--mode` flag and sessions_db-mode mirroring still deferred.
- ADR-0029 verification/assurance phase (per-artifact) still future work.

## Notes

- Node-scoped stream types (`info`/`debug`/`state`/`usage`) require
  `node`; `context` is graph-level with no `node`.
- `graph.update_state()` emits no stream events, so mode changes ride the
  next query.
- Hydration decision: mode stays in checkpointed graph state (single
  source of truth, ADR-0032); nothing copied into the sessions db -- an
  unsent query is not a chat yet. After a reload, `requested` is restored
  from the checkpoint; an empty `query_extra` would otherwise send the
  server default and silently reset the whole-object `Mode` field.
- `POST /query` deliberately returns no context (bare `ainvoke`, no
  `values` events); callers use `/query/stream` or the context GET --
  rationale noted in the `run_graph_invoke` / `run_query` docstrings.
- Session offset: PR 4-7 built on the earlier session log
  `.agents/2026-09-02-1629.md` (utils/apps split, ADR-0031).

## References

- `devdocs/adr/0030-agent-operating-modes-and-assurance-levels.md`
- `devdocs/adr/0031-apps-own-api-and-ui-composition.md`
- `devdocs/adr/0032-graph-context-events-and-state-ownership.md`
- LangGraph event streaming docs (LangChain)
