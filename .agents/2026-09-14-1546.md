# Session: 2026-09-14-1546

**Authoring agent:** opencode
**Model:** deepseek-v4.1-flash

## Objective
Resolve the `base.py` TODO about empty LLM output (HuggingFace sometimes
returns a blank successful message): retry transient empties and ensure a
persistent empty answer surfaces a clear message instead of a blank reply.
Changes not committed; left for user review.

## Completed
- `klea_utils/llm.py`: added `is_output_empty(output)` (blank `AIMessage`
  content, or blank structured `raw`; parsed-only counts as non-empty).
- `klea_utils/nodes/base.py`:
  - `MAX_EMPTY_OUTPUT_RETRIES = 2` and a bounded plain re-invoke in
    `_invoke_with_retries` when a successful response is empty.
  - `_process_output` now returns `_get_default_error_result()` when an
    empty result is structured (typed fail-closed default); non-structured
    nodes are unchanged.
  - `_is_empty_result` delegates its blank-`AIMessage` branch to
    `is_output_empty` (single source of truth for blank-content checks).
  - Removed the resolved TODO.
- User-facing fallbacks: `AnswerGeneral` (utils) and RAG `AnswerFromContext`
  now return "Something went wrong ... Please retry." on an empty answer
  (`EMPTY_ANSWER_FALLBACK` in each module).
- Consolidated the fallback into one shared `EMPTY_RESULT_FALLBACK` in
  `klea_utils.nodes.base` (used by both nodes and the agent delivery node).
- Audited agent answer producers: added a last-resort guard in the agent
  `AnswerUser` delivery node so a blank `message_for_user` (e.g. the router
  chose `chat` but emitted an empty answer) never reaches the user.
  `AnswerFromResults` already had a deterministic fallback; `ModeInformer`
  and the guard `FixedAnswer` are non-empty by construction; the planner and
  evaluator are structured fail-closed, not user-facing.
- Tests: `is_output_empty` cases, empty-retry success/exhaustion,
  `_process_output` structured-empty fallback, `AnswerGeneral` blank case,
  agent `AnswerUser` blank case, new RAG `test_answer_from_context.py`.
  `CHANGELOG.md` Fixed bullets (utils/rag and agent).

## Errors and Lessons
- The TODO's literal suggestion ("return a retry message" from `_invoke_llm`)
  is unsafe: structured/fail-closed nodes (`RouteSchema` task, `Planner`
  unplannable, `EvaluationSchema`) rely on their all-default result, and
  `GuardNode._get_default_error_result()` returns a bare `str` while its
  `_update_state` reads `result.content`. Substitution is therefore scoped
  to structured nodes; non-structured nodes handle emptiness themselves.
- Empty substitution improves fail-closed behaviour: `EvaluationSchema`'s
  default `evaluation` is `plan_done`, so an empty evaluator result now
  correctly becomes `need_replan` via `_get_default_error_result()`.

## Open Questions
- `agent_pkg` has 11 pre-existing test failures: tests construct
  `EvaluationSchema(next_step=...)` and read `.next_step`, but the schema
  field was renamed to `evaluation`. Neither the tests nor the schema are
  touched by this session; decide whether to update them separately.

## Notes
- Verification: utils 934 passed / 10 deselected; rag 68 passed; agent 84
  passed / 11 failed (all pre-existing, above). `ruff check`/`format` clean
  in utils and rag. `ty check` adds no new diagnostics to changed files (two
  pre-existing `llm.py` `pydantic_object` errors remain).
- Left the user's in-progress `agent_pkg/example-configs/*.env` edits
  untouched. Running the utils store tests dirties
  `vector_stores/nml-elife/` artifacts; reverted after the run.

## References
- `devdocs/adr/0035-general-operational-agent-path.md`,
  `devdocs/system/agent-general-path-control-flow.md`
- `utils_pkg/klea_utils/nodes/base.py`, `utils_pkg/klea_utils/llm.py`
